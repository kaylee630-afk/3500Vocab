# Project instructions

- After every code change, run the Xcode build and make sure it succeeds before committing.
- Commit all changes to the local Git repository.
- Push the `main` branch to `origin/main` after every successful build.
- Keep the user interface white, clean, and lightly animated as originally requested.
- Do not leave verified changes uncommitted or unpushed unless the user explicitly asks to hold them.
